# FastAPI stack
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Database
sqlalchemy[asyncio]>=2.0.28
asyncpg>=0.29.0
alembic>=1.13.0
psycopg2-binary>=2.9.9

# Redis
redis[hiredis]>=5.0.2

# Auth / security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9

# HTTP
httpx>=0.27.0

# Structured logging
structlog>=24.1.0

# Internal packages (installed separately via make install)
# scada-shared
# scada-analytics
# scada-connectors

# Kafka
aiokafka>=0.10.0

# Background tasks
celery[redis]>=5.3.6
